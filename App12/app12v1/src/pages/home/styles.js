import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  borda: {
    backgroundColor: 'gray',
    flex: 1,
  },
  titulo: {
    color: 'red',
    fontSize: 35,
    textAlign: 'center',
  },
})
export { styles }